node_modules/
dist/
.expo/
*.log

# local signing credentials — never commit
credentials.json
credentials/

# generated native projects (Expo CNG) — regenerated by prebuild/EAS, never committed
ios/
android/
